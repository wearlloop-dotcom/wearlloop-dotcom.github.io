# รายงานออดิทระบบ LLOOP — กรกฎาคม 2026
## ตรวจความพร้อมก่อนสร้าง "กำแพงกันคู่แข่ง 5 ด้าน"

ออดิทโดยไล่อ่านโค้ดทุกโมดูลที่เกี่ยวข้อง (5 สาย ครอบคลุม ~50 หน้า) หมายเหตุ: ตัว RPC/ฐานข้อมูลอยู่ฝั่ง Supabase นอก repo นี้ — ข้อสรุปฝั่ง backend อ่านจาก contract ที่หน้าเว็บเรียกใช้

---

## สรุปผู้บริหาร

โครงสร้างระบบ**แข็งแรงเกินมาตรฐานร้านเช่าชุดมาก** — มี gateway กันปลอมตัว (LINE idToken), ระบบเครดิต/สมาชิกสมบูรณ์, เอกสารหลักฐานเชิงกฎหมายครบ (KYC/PDPA/e-sign/case file) แต่ปัญหาซ้ำ ๆ ที่พบทั้ง 5 สาย สรุปได้เป็น 3 คำ:

1. **เก็บแล้วไม่ใช้** — ข้อมูลถูกเก็บครบแต่ไม่ถูกนำไปคำนวณ/แสดงผล (สัดส่วนสะโพก-น้ำหนัก, สไตล์ที่ลูกค้ากรอก, wash count, เกรดชุด)
2. **สร้างแล้วไม่เชื่อม** — โมดูลดี ๆ ลอยเป็นเกาะ (รับซื้อไม่ไหลเข้าคลัง, เฝ้าตลาดไม่เชื่อม intake, รีวิว 3 ระบบคนละมาตรฐาน)
3. **โชว์แล้วไม่จริง** — คำสัญญาหน้าเว็บยังไม่มีข้อมูลรองรับ (ป้าย NFC ชี้หน้าเปล่า, "ซักทุกตัว" ตรวจสอบไม่ได้, เลขแทร็กว่างเปล่า)

### คะแนนความพร้อมต่อกำแพง

| กำแพง | ความพร้อม | สรุปสั้น |
|---|---|---|
| 1. ข้อมูลไซซ์/โปรไฟล์ | ★★★☆☆ 60% | เก็บครบ คำนวณ fit ได้ แต่ครึ่งหนึ่งของข้อมูลไม่ถูกใช้ · ปฏิทินงานลูกค้าเพิ่มเองไม่ได้ |
| 2. Closet Cash (ซัพพลาย) | ★★☆☆☆ 35% | ฝั่งผู้ขายส่งของได้ แต่ฝั่งร้าน**ไม่มีหน้าจัดการเลย** · ไม่มี consignment · ไม่เชื่อมคลัง |
| 3. โลจิสติกส์/ความหนาแน่น | ★★☆☆☆ 40% | เฟส 2 (สาขา/จุดรับ-ส่ง) สร้างเสร็จรอเปิดสวิตช์ แต่การส่งของยังแมนวล ไม่มีเรทตาราง/ตัวนับพัสดุ |
| 4. รีวิว "คนไซซ์เดียวกัน" | ★★★☆☆ 55% | Loop Looks + รีวิว + affiliate ครบเครื่อง แต่**รีวิวไม่เก็บสัดส่วนผู้รีวิว** และ fit 3 มาตรฐานไม่เข้ากัน |
| 5. สุขอนามัยตรวจสอบได้ | ★★☆☆☆ 40% | ระบบซัก/QC หลังบ้านมีจริง แต่ช่องสำคัญส่งค่าว่าง · ลูกค้ามองไม่เห็นอะไรเลย |

---

## ซ่อมแล้ววันนี้ (frontend — commit นี้)

| # | อาการ | ไฟล์ | การแก้ |
|---|---|---|---|
| 1 | ป้าย NFC เขียน URL `lloop.app/?g=` ที่ไม่มีหน้าไหนอ่าน → ลูกค้าแตะแล้วเจอหน้าเปล่า | `nfc.html` | ชี้ไป `g.html?c=<code>` (scheme ที่ใช้งานได้จริง) |
| 2 | ป้ายพิมพ์เข้ารหัส QR เป็น `lloop.app/g/` → 404 (DNS ยังไม่ผูก) | `labels.html` | ใช้ base เดียวกับ `care-label.html` ที่ออนไลน์จริง |
| 3 | สติกเกอร์ shipout เข้ารหัส QR เป็น `lloop.app/g/` → 404 (2 จุด) | `shipout.html` | เปลี่ยนเป็น `g.html?c=` |
| 4 | ลิงก์จากงานวันนี้ `shipout.html?code=x` เปิดแล้วไม่โหลดให้ ต้องพิมพ์ซ้ำ | `shipout.html` | อ่าน `?code=` แล้ว auto-load |
| 5 | `settings.html` โหลด `../liff/config.js` (path ผิด, 404) → ใช้ค่า fallback เงียบ ๆ | `settings.html` | ชี้ `config.js` ที่ root |
| 6 | AI ช่วยกรอก intake ส่ง header `Bearer<key>` (ขาดเว้นวรรค) → ล้มเงียบทุกครั้ง | `intake.html` | เติมเว้นวรรค |
| 7 | ส่งของด้วย EMS แล้วลูกค้าไม่มีลิงก์ติดตามพัสดุ (map ไม่มี key `ems`) | `app.js` | เพิ่ม `ems` → Thailand Post |
| 8 | CSV import/export ตกคอลัมน์ `size` / `condition` / `acquisition_cost` → ข้อมูลทุน-เกรดหายเวลานำเข้าเป็นชุด | `csv.html` | เพิ่ม 3 คอลัมน์ (RPC `intake_garment` รองรับอยู่แล้ว) |
| 9 | `washCount` + `grade` ถูกดึงมาแล้ว (api.js) แต่ไม่มีหน้าไหนแสดง | `g.html` | เพิ่มบรรทัดความสะอาด "ซักและตรวจสภาพก่อนส่งทุกครั้ง · ดูแลมาแล้ว X รอบ · เกรด A" บนหน้าสแกน — ก้าวแรกของกำแพงที่ 5 |

---

## งานที่ต้องทำต่อ (ต้องแตะ Supabase/RPC — เรียงตามผลตอบแทน)

### P0 — เสียบสายไฟที่หลุด (เล็ก แต่ผลใหญ่ เพราะข้อมูลเริ่มสะสมตั้งแต่วันแก้)

1. **รีวิวแนบสัดส่วนอัตโนมัติ** — `add_review` เพิ่ม `p_height`, `p_size` โดยดึงจากโปรไฟล์ผู้รีวิว (ฝั่งลูกค้าไม่ต้องกรอกเพิ่มเลย) → นี่คือฐานของ "รีวิวจากคนไซซ์เดียวกับคุณ" ทุกวันที่ยังไม่แก้ = รีวิวที่เสียเปล่า
2. **บันทึกวิธีซัก + ผู้ตรวจจริง** — ทุก call site ส่ง `p_method:null`, `p_handler:null` (`laundry.html:171`, `laundry-shops.html:109`, `care_qc`) ทั้งที่ timeline พร้อมแสดง → เพิ่ม UI เลือกวิธีซัก + ส่งชื่อ staff จาก gateway
3. **ปฏิทินงานของลูกค้า** — ตาราง `customer_events` มีครบ (วันงาน/โอกาส/dress code/ธง notified) ขาดแค่ RPC `add_customer_event` + ฟอร์ม "เพิ่มงานของฉัน" + ให้ `quiz.html` บันทึกโอกาส/วันที่ที่ลูกค้าตอบอยู่แล้ว (ตอนนี้ทิ้งข้อมูลทุกครั้ง)
4. **สไตล์ที่ลูกค้ากรอกให้มีผลจริง** — `saveProfile` เขียน `prefs.styles/occasions/fav_colors/avoid_colors` แต่ `personalScore()` อ่าน `style_profile` (คนละ object) และ `avoid_colors` เทียบ hex กับ free text ไม่มีวันแมตช์ (`app.js:248-251, 3551-3555`) → รวม schema ให้เป็นหนึ่ง

### P1 — สร้างเครื่องยนต์ Closet Cash (กำแพงที่ 2)

5. **หน้า ops จัดการรับซื้อ** — ตอนนี้คิว "รับซื้อรอจัดการ" ใน `today.html` กดไม่ได้ (`rowLink(null)`) และไม่มีหน้าตีราคา/ตอบรับ/ออกใบรับซื้อ/จ่ายเงินเลย ทั้งที่ฝั่งผู้ขายกรอก KYC+เลขบัญชีมาครบ → ต้องมี RPC ชุด `seller_offer_review/accept/reject` + จ่ายผ่าน PromptPay (`promptpay.js` มีอยู่แล้วแต่ไม่ถูกใช้)
6. **สะพานรับซื้อ→คลัง** — offer ที่รับแล้วต้อง prefill เข้า `intake_garment` อัตโนมัติ (ตอนนี้ต้องคีย์ใหม่หมด) + ผูก id ระหว่าง acquisition กับ garment เพื่อวัดต้นทุน-ผลตอบแทนรายชุด
7. **เปิดประตูฝั่งลูกค้า** — `seller.html` เข้าถึงได้จากเมนู staff เท่านั้น ไม่มีลิงก์จากหน้าลูกค้าแม้แต่จุดเดียว → เพิ่ม entry จาก index/home + แคมเปญ "วันเปลี่ยนตู้"
8. **มิเตอร์ซัพพลาย** — สัดส่วนคลังที่มาจากรับซื้อ, ต้นทุนเฉลี่ย/ชุด, conversion ผู้ขาย (เป้าที่คุยไว้: >50% ของคลังจาก Closet Cash)

### P1 — โลจิสติกส์ก่อนคุยขนส่ง (กำแพงที่ 3) — ดู `logistics-pack.html` ประกอบ

9. **ช่องกรอกเลขแทร็ก** — `tracking_no/courier/eta` ลูกค้าเห็นค่าว่างเพราะ**ไม่มีหน้า ops ไหนกรอกได้เลย** → เพิ่มใน shipout flow (สแกน/พิมพ์เลขตอนแปะป้าย)
10. **ตัวนับพัสดุ/เดือน** — ไม่มี metric นี้ในระบบ (สิ่งแรกที่ขนส่งถามตอนต่อรอง) → เพิ่มใน `owner_cockpit`
11. **ตารางเรทขนส่ง** — ค่าส่งฮาร์ดโค้ดสูตรเดียวกระจาย 4 ไฟล์ (`api.js:563`, `g.html`, `pay.html`, `group-checkout.html`) จะ drift จาก `quote_rental` → ย้ายเข้า `app_settings` เป็น rate table ต่อเจ้า/โซน รองรับเรทที่เจรจาได้
12. **ใบส่งกลับ prepaid** — ขากลับตอนนี้ลูกค้าจ่ายเอง/ไม่มีแทร็ก → รวมใน deal ขนส่ง (volume เพิ่มเท่าตัว = อำนาจต่อรอง + คุมประสบการณ์คืนชุด)

### P2 — ยกระดับเป็นกำแพงเต็มรูป

13. **fit ใช้สะโพก/น้ำหนัก** — `fitConfidence()` ใช้แค่อก/เอว ทั้งที่เก็บสะโพก-น้ำหนัก-ไซซ์มาแล้ว และ `g.hip` ไม่ถูกคิด → แม่นขึ้นก่อนประกาศ "การันตีพอดีตัว"
14. **รวมมาตรฐาน fit เป็นหนึ่ง** — ตอนนี้ 3 ระบบ: `tight/perfect/loose` (รีวิว), `-2..+2` (`review.html` ที่ตายแล้ว), `true/large/small` (Loop Looks) — สรุปหน้าชุด ("ชุดนี้ออกใหญ่") คิดจาก Looks เท่านั้น รีวิวจำนวนมากกว่าถูกทิ้ง
15. **ตัดสินใจ `review.html`** — หน้า orphan ไม่มีลิงก์เข้า, RPC `submit_review` ไม่มีใครเรียก แต่ schema รวยที่สุด (occasion/feel tags) → เชื่อมหรือลบ อย่าปล่อยให้ drift
16. **hygiene พับลิก** — RPC `garment_hygiene_public(p_code)` (เฉพาะ ซัก/ฆ่าเชื้อ/QC ผ่าน — ไม่โชว์ชื่อ staff/ค่าปรับ) + ส่วน "ประวัติชุดตัวนี้" บน `g.html` + เพิ่มขั้นตอน/ฟิลด์ฆ่าเชื้อใน care cycle (ตอนนี้ไม่มีคำว่า sanitize ในระบบเลย) → จบด้วยแบรนด์ "มาตรฐานความสะอาดระดับสถานพยาบาล" (ยืมเครดิตเยียวยา)
17. **ส่วนลดกลุ่มสองที่ไม่ตรงกัน** — `group-checkout.html` ให้ 2/3/4 คน = 5/10/15% แต่การ์ดเพื่อนเจ้าสาวใน `family.html` บอกแบน 5% — ต้องเลือกหนึ่งเดียว (ตัวเลขจริงอยู่ที่ DB `group_discount_pct`)

### บั๊กรอง (แก้เมื่อสะดวก)
- ปุ่ม "เตือนเพื่อน" ใน `event.html` เป็นปุ่มหลอก (แค่ alert)
- `customer_events` กรอง `notified=false` → งานหายจากแบนเนอร์ทั้งที่ยังไม่ถึงวันงาน
- `ops-looks.html`/`live.html` ฝัง Supabase URL/key ตรง ๆ แทน `config.js` (drift risk)
- affiliate ครีเอเตอร์เขียน `customer_touchpoints` ตรงจาก anon client (ไม่มี RPC คุม anti-fraud ทั้งที่จ่ายเครดิต 10%)
- `wishlist.html` ชื่อไฟล์กับเนื้อในไม่ตรง (จริง ๆ คือ waitlist + โหวตเทรนด์)
- เวอร์ชัน cache-buster (`?v=`) ไม่สม่ำเสมอระหว่างหน้า

---

## อัปเดตสถานะ (หลังออดิท — ทำเพิ่มใน branch เดียวกัน)

ทำเสร็จแล้วฝั่ง frontend: **ข้อ 5 บางส่วน** (หน้า acquisitions.html + ลิงก์คิว + intake prefill — รอ RPC จาก SQL), **ข้อ 7 บางส่วน** (การ์ด Closet Cash ใน links.html — ส่วน seller.html ยังติด ops gate ต้องตัดสินใจฝั่ง gateway), **ข้อ 13** (fit ใช้สะโพกแล้ว — ต้อง sync SQL ตาม §4.5), **ข้อ 14 บางส่วน** (หน้าชุดแสดง "รีวิวจากคนตัวใกล้คุณ" — รอ RPC §1), **ข้อ 17** (ส่วนลดกลุ่ม family.html ตรงกับ checkout แล้ว: 2→5% · 3→10% · 4→15%), ปุ่มเตือนใน event.html เปลี่ยนเป็นคัดลอกข้อความเตือน+ลิงก์จริง, ops-looks/live ใช้ค่า config.js แล้ว, ปฏิทินงานลูกค้า (my-events.html + ลิงก์จากโปรไฟล์+ควิซ — รอ RPC §3), laundry บันทึกวิธีซัก+ผู้ทำแล้ว (รอ DB เขียนตาม §2)

ยังค้างทั้งหมดฝั่ง backend: รัน `supabase-p0-moats.sql` + แพตช์จุด [CHECK] — โดยเฉพาะ intake_garment รับ acquisition_id (§4) และ n8n แจ้งเตือนก่อนวันงาน (§3)

## ลำดับแนะนำ (ผูกกับแผนเร่งเครื่อง 18 เดือน)

- **เดือนนี้ (พร้อมเลเวล 1):** P0 ทั้ง 4 ข้อ — ทุกวันที่ช้า = ข้อมูลรีวิว/ซัก/ปฏิทินที่ไม่ได้สะสม
- **เดือน 1–3:** P1 Closet Cash (ข้อ 5–8) — ต้องติดเครื่องก่อนสต็อกโตช่วงเลเวล 2
- **เดือน 2–4:** P1 โลจิสติกส์ (ข้อ 9–12) + เริ่มคุยขนส่งด้วย `logistics-pack.html` เมื่อ volume แตะ ~500 กล่อง/เดือน
- **เดือน 4–9:** P2 — fit guarantee ประกาศได้เมื่อข้อมูลรีวิว+สัดส่วนแน่นแล้ว (raw data เริ่มไหลตั้งแต่ P0)
